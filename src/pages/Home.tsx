import React from "react";
import SearchBar from "../components/SearchBar/index";

interface Props {}

const Home = (props: Props) => {
  return (
    <div>
      <SearchBar />
    </div>
  );
};

export default Home;
